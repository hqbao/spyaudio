# Recording hider

This library is to hook into SpringBoard and disable microphone indicator

1. Make sure that the device has been install ElleKit and the folder /Library/MobileSubstrate/DynamicLibraries/ is exist

2. Run command to deploy in iPhone
./deploy.sh

Please note you should change root password accordantly, the default password is "000000"
